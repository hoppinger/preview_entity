preview_entity.preview_url:
  path: '/api/preview/{entity_type}/{entity_id}/{revision_id}'
  defaults:
    _controller: '\Drupal\preview_entity\Controller\PreviewEntityController::build'
  requirements:
    _permission: 'access content'
  options:
    _auth: ['api_key_auth']